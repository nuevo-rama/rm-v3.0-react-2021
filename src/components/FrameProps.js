import React from 'react';

function FrameProps(props) { //LA FUNCION SE LLAMA COMO EL COMPONENTE
                        //SE PASA POR PARAMETRO PROPS

    
    

    return (
      <div>

        <h2> {props.nombre} </h2>
        
      </div>
    );
  }

export default FrameProps;